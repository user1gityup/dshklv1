/** Host loader entry for the browser-only council budget panel. */

export type { BudgetKey } from './client/locales.ts'

/** Provides no host-side behavior; the surface lives in the client face. */
export function apply(): void {}
