/** Host loader entry for the browser-only Claude quota panel. */

export type { QuotaKey } from './client/locales.ts'

/** Provides no host-side behavior; the surface lives in the client face. */
export function apply(): void {}
